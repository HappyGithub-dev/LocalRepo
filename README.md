# Project - Object Detection

## Things done / Concepts involved 
- Import necessary dependencies in the colab notebook . 
- A file named coco.name was imported . This file contains the names of various objects which are stored as a list .
- A Neural Network was introduced in order to introduce the scale factor and swap the color scale of the image from RGB to BGR . 
- Lastly the rectangle was initialised which would detect the object ( in this case human ) with its 2 diagonal ends .



